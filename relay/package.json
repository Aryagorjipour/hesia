{
  "name": "hesia-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-only Hesia relay — SMTP email and MCP bridge (localhost binding)",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun --watch index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "hono": "^4.7.11",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemailer": "^6.4.17"
  }
}